# LangChain dependencies
langgraph==1.0.1
langchain==1.0.2
langchain-community==0.4
langchain-core==1.0.1
langchain-ollama==1.0.1
langchain-text-splitters==1.0.0

# PDF parsing
pypdf==6.1.3

# UI framework
streamlit==1.42.0
streamlit-agraph==0.0.45

# Knowledge graph
networkx==3.6.1

# Utilities
tenacity==9.1.4
python-dotenv==1.2.2

# Optional: LlamaCPP provider support (LLM_PROVIDER=LlamaCPP) lives in
# requirements-llamacpp.txt — it pulls in the large compiled llama-cpp-python
# package and isn't needed for the default Ollama/Azure setup.
